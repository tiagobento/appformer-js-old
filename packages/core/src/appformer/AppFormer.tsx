import * as React from "react";
import { Element, Component } from "../core/Component";
import { Core } from "../core/Core";

export class AppFormer {
    // public root: AppFormerRoot;
    //
    // public init(app: Component, container: HTMLElement, callback: () => void) {
    //     new Core().init(<AppFormerRoot exposing={ref => (this.root = ref())} core={this} app={app} />, container, callback);
    //     return this;
    // }
}
