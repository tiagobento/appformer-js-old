import * as AppFormer from "appformer";
import * as ShowcaseComponents from "showcase-components/ShowcaseComponents";
import * as ConsoleComponents from "appformer/console";

AppFormer.register({ ...ShowcaseComponents, ...ConsoleComponents });
